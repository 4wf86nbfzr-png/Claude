node_modules/
.next/
out/
build/
.env
.env.local
.env.*.local
storage/
!storage/.gitkeep
# Beispieldateien entstehen beim Seed (npm run seed)
beispiele/
*.tsbuildinfo
next-env.d.ts
coverage/

# Die eigenständige Demo-Fassung (rund 5 MB) entsteht aus Dateien, die
# ohnehin im Repo liegen: `npm run demo:einzeldatei`. Sie mitzuführen
# hiesse, bei jedem Neubau einen weiteren 5-MB-Blob in die Historie zu
# legen – und die wird man nicht wieder los.
demo/hst-planer-demo.html
